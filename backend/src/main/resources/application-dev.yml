spring:
  datasource:
    url: jdbc:sqlite:${DB_PATH:../data/ai_api_platform_dev.db}
    hikari:
      minimum-idle: 1
      maximum-pool-size: 2
      connection-timeout: 30000

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: ${JWT_SECRET:yswionRCkQglQbB3AlYOwrJ+eo+pR0FciMC5lWVtxWI=}

encryption:
  key: ${ENCRYPTION_KEY:BzYuRUcVqTR09G9suRDaA05ewMoxFXe8}

admin:
  security:
    enable-rate-limit: ${ADMIN_ENABLE_RATE_LIMIT:false}
    enable-login-lock: ${ADMIN_ENABLE_LOGIN_LOCK:false}
